import { Routes } from '@angular/router';
import { UpdateProfileComponent } from './update-profile.component';

export const UpdateProfileroutes: Routes = [

    {
        path:'update-profile',
        component: UpdateProfileComponent,
    }

];